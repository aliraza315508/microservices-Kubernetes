apiVersion: v1
kind: Service
metadata:
  name: currency-exchange
  namespace: default
  labels:
    app: currency-exchange
spec:
  selector:
    app: currency-exchange
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
  type: LoadBalancer
